# 验收记录

日期：2026-09-15。

## 已实际通过

- 环境：Windows + Docker Desktop，Linux 容器；仅 Web 端口 8080 暴露，PostgreSQL 与 API 保持在 Compose 内网。
- 冷启动：从空数据库执行 Docker Compose 统一启动命令成功；2 个已提交迁移按顺序应用，管理员与资料初始化成功，db、api、web 均进入 healthy，init 成功退出。
- 首次数据库竞态：PostgreSQL 官方镜像初始化临时服务结束时出现过一次 P1001；初始化器现在只对该瞬时错误限次重试，永久迁移错误仍立即失败。
- 失败路径：管理员初始密码少于 12 位时 Compose 返回失败，API 与 Web 不启动；选择 oss 但缺少 OSS 配置时 API 拒绝启动。
- 类型与构建：API、Web 类型检查通过，NestJS 与 Vite 生产构建通过。
- 单元测试：5 项通过，覆盖真实日历日期、闰日、年龄/到家天数、上传边界、密码哈希和 OSS 缺少配置。
- 接口与权限：42 项检查通过，覆盖登录会话、请求来源校验、稳定排序、搜索筛选、草稿/私密隔离、相册复用和删除、封面约束、新上传媒体在记录保存前不可公开等行为。
- 图片：真实 PNG 上传后生成 WebP 缩略图与展示图；伪装图片、超限文件被拒绝。
- 视频：测试生成的 MP4/H.264 经 ffprobe 校验并由 ffmpeg 生成封面；媒体 Range 请求返回 206，不兼容规则返回明确错误。
- 真实 OSS：使用实际私有 Bucket 完成凭据及写入/读取/删除权限检查；API 返回 V4 HTTPS 预签名 PUT，Bucket CORS 允许当前网站 Origin，服务端成功回读并生成 WebP 展示图和缩略图，公开接口签发 5 分钟 GET 地址且可读取。联调测试记录和对象已自动清理。
- 存储切换：`.env` 已设为 `STORAGE_DRIVER=oss`，Compose 全链路重建并健康；切换前将本地卷中 1 个既有媒体对应的 3 个文件复制到 OSS，本地副本保留，并补齐对象 Content-Type 与私有缓存元数据。
- 持久化：保留一组私密测试记录后强制重建全部 Compose 容器，原管理员、补录日期、2 个媒体文件及下载内容均保持完整；验证后已删除测试记录和媒体，当前主库记录/媒体/相册均为 0。
- 备份恢复：生成 backups/bobo-20260915-092326.dump，恢复到独立 bobo_restore 数据库；恢复前后的记录数与媒体元数据数均为 3/3，运行中的 bobo 主库未被覆盖，验收后已删除独立恢复库。
- 浏览器：实际查看首页和管理端；完成管理员登录、草稿保存及正文预览。H5 适配后在 375、390、430px 检查首页、时间线、故事详情、相册、关于页和后台登录页，在 768px 检查双栏平板布局，均无横向溢出；手机端底部导航、筛选控件和触控尺寸正常。首页在 1280、1440、1920px 下继续使用原桌面布局且无横向溢出，空状态明确说明等待啵啵真实照片，未展示虚构成长记录。
- 依赖审计：生产依赖审计为 0 个漏洞。
- 部署脚本：Windows 一键部署入口实际运行成功；3 个 PowerShell 脚本完成语法解析，4 个 Bash 脚本已分别通过容器语法检查或真实 Ubuntu 执行验证。
- Ubuntu 与 ACR：在真实 Ubuntu ECS 上完成发布。华东 2（上海）ACR 的 `bobo-api` 与 `bobo-web` 两个私有仓库通过绑定 GitHub 的 `main` 分支分别构建 `Dockerfile.api` 和 `Dockerfile.web`；服务器登录实例 Registry、拉取两个 `latest` 镜像、执行初始化和健康等待后成功对外发布。该结果由实际服务器操作确认，完整过程见 `docs/ubuntu-acr-deployment.md`。

## 尚待真实环境验收

- 未提供正式域名和证书；HTTPS 配置已准备，证书签发、续期和公网访问仍待服务器验收。
- 浏览器中的原生文件选择器在最后一次容器重启后没有再次自动操作；真实图片与视频上传闭环已由同一运行实例的接口集成测试验证。

## 环境事件

压力测试隔离 Compose 项目时，Docker Desktop 4.83.0 因自身 dockerInference 残留套接字停止响应。重启 Docker Desktop 后其本地引擎数据被重置；当时只有自动测试数据，没有啵啵的真实内容。随后从全新引擎重新完成冷启动、媒体闭环、普通容器强制重建持久化和备份恢复验收。这个事件不等同于普通 docker compose down 或容器重建，也再次说明数据库备份与本地媒体备份需要配套执行。

API 最终使用固定摘要的官方 Node Alpine 镜像，视频功能由镜像内真实 ffmpeg / ffprobe 提供。项目未初始化演示成长记录。
